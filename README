# SystemJS jsx plugin demo

To get started run:

```
npm install && jspm install && npm run
```

Then open `http://localhost:9001` and follow the instructions

Try running `System.import('demo.jsx!').then(console.log.bind(console));` in the console
